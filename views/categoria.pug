extends ./layout/app

block contenido 
    section(class='w-full pt-10')
        h2(class='text-center text-4xl font-extrabold ')= pagina 

        if propiedades.length 
            div(class='grid grid-cols-[repeat(auto-fit,minmax(310px,1fr))] sm:grid-cols-[repeat(auto-fit,minmax(420px,1fr))] gap-5 justify-items-center sm:px-10 ')
                each propiedad in propiedades
                    include ./templates/card
        else 
            p(class='text-center text-gray-800 text-sm py-10') No hay publicaciones 